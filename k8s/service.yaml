# Unit 4 - Cloud Computing and DevOps  -  OTHM K/650/7997
# Artefact P4.10   Assessment criteria: AC 3.5 - Kubernetes services
# ClusterIP Service fronting the Deployment
#
# Vera Cree  |  Candidate 240301062  |  CIPS Centre DC2401845

# ---------------- SERVICE (AC 3.5) ----------------
apiVersion: v1
kind: Service
metadata:
  name: skyforge-api
  namespace: production
spec:
  type: ClusterIP                           # internal only; ALB fronts via Ingress
  selector: { app: skyforge-api }
  ports:
    - { name: http, port: 80, targetPort: 8080, protocol: TCP }
